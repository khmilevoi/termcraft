/.superpowers/
/design-prompts/
.claude
node_modules/
*.exe
/.worktrees/
/dist/

# WP-11 (gap B7): generated by `scripts/gen-tsc-libs.ts` (`postinstall`) from the installed
# `@typescript/typescript-win32-x64` package — reproducible, never hand-edited, and the
# `.d.ts.txt` renaming works around TypeScript's own TS2846 ("a declaration file cannot be
# imported") which fires on a literal `.d.ts` extension regardless of import attributes.
/scripts/tsc-libs.generated.ts
/scripts/tsc-lib-sources/

HANDOFF.md
