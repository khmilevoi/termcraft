/**
 * `ui/theme` — the app-shell palette and text-style helpers. The single source of shell
 * colour/attribute values, taken verbatim from the design system (`pal`/`lightPal`).
 */
export type { ShellPalette, ShellTextStyle, ShellToken } from "./types";
export {
  BLINK_CURSOR,
  CURSOR_GLYPH,
  LIGHT_SHELL_PALETTE,
  SHELL_PALETTE,
  shellAttrs,
} from "./model/palette";
